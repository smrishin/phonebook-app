{
  "name": "phonebook-app",
  "version": "1.0.0",
  "description": "Create, delete and edit contacts and websocket are implemeted for realtime connection",
  "main": "index.js",
  "scripts": {
    "dev-client": "cd web && npm run dev",
    "dev-server": "cd server && .\\venv\\Scripts\\activate.bat && python app.py",
    "create-tunnels": "node localtunnel/create-tunnels.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "localtunnel": "^2.0.2"
  }
}
